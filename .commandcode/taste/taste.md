# Taste

- Wants concept questions answered as durable Markdown notes: one `.md` file per concept, saved inside an `IQ_Nodes/` folder (created if missing), named `<Concept_Name>.md` in PascalCase with underscores. Confidence: 0.65
- Prefers explanations kept simple and beginner-friendly, mirroring the style/format of an existing example file in the repo (e.g. a small JS code block plus a plain-language explanation). Confidence: 0.6
- Learning JavaScript basics through small, single-concept example files (e.g. `Let_Concept.js` containing only `let x = 1; console.log(x);`). Confidence: 0.5
- Wants completed work committed and pushed to their GitHub repo (e.g. `github.com/bharath-kumar-venukanti/LP3x.git`) as part of finishing the task, rather than being handed manual git commands. Confidence: 0.75
